putchar := proc(char: s32) -> s32 #extern "putchar"

ArrayLength :: 2 * 3 + 1
array: [ArrayLength]u8

{
    i := 0
    while i != test.c[5] - 1 {
        array[i] = 69
        i = i + 1
    }
    array[i] = 0
}

{
    i := 0
    while array[i] != 0 {
        putchar(cast(s32) array[i])
        i = i + 1
    }
}
